import { BASE_URL } from '@constants/config';

export const ENDPOINTS = {
  BASE_URL: BASE_URL, // https://newsapi.org/v2
  TOP_HEADLINES: '/top-headlines',
  EVERYTHING: '/everything',
  SOURCES: '/sources',
};
